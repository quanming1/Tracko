1、ui不能使用强烈的颜色，能不使用颜色就不使用颜色
2、信息密度要高
3、禁止使用emoji
4、别他妈在页面上写一些标注性质的文字，例如：说明、提示、警告等
5、所有展示响应式更新的组件都必须使用FlashBox包裹，当组件数据变化时会自动闪烁（黑底白字200ms），通过deps参数传入依赖项
6、自研Store使用方法:
   - 单组件Store: const [store] = useState(() => makeAutoObservable(new StoreClass()))
   - 全局Store: createStores({ store1, store2 }) 返回 { stores, useStores }
   - 组件中通过 const { store1 } = useStores() 访问，自动追踪访问的字段，精准订阅更新
   - Store类定义: 普通class，getter自动成为computed，无需装饰器
   - 支持深层嵌套对象响应式追踪67